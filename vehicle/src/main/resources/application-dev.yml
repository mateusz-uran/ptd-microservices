server:
  port: '0'

spring:
  data:
    mongodb:
      username: mateuszuran
      database: mongo-vehicle
      password: password123
      port: '27017'
      host: localhost
  application:
    name: vehicle-service

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8181/auth/realms/ptd-auth
