version: '3.3'

services:
  #mysql service
  db:
    image: mysql:latest
    container_name: ptd-mysql
    environment:
      - MYSQL_ROOT_PASSWORD=password
      - MYSQL_USER=root
    ports:
      - "3306:3306"
    restart: unless-stopped